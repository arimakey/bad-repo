# LangGraph AI Agent

A feature-rich AI agent project using LangGraph, Google Gemini, and FastAPI.

## Features
- Multi-agent architecture
- Live video streaming and analysis
- Risk assessment and automated actions

## Setup
1. Copy `.env.example` to `.env` and fill in your keys.
2. Install dependencies: `poetry install`
3. Run the server: `make serve`
